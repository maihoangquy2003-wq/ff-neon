// Script Test New
var body = $response.body;
if (body) {
    // Code Antena & Aim vĩnh viễn
    body = body.replace(/AimBot/g, "AimLock");
    body = body.replace(/Headshot/g, "AlwaysHead");
    $done({ body });
} else {
    $done({});
}
